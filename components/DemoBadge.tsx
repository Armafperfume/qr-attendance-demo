export default function DemoBadge() {
  return (
    <div
      aria-hidden
      className="no-print pointer-events-none fixed bottom-3 right-3 z-50 select-none rounded-full border border-line bg-paper/80 px-2.5 py-1 text-[10px] font-semibold uppercase tracking-[0.18em] text-muted/80 backdrop-blur"
    >
      Demo · nothing stored
    </div>
  );
}
